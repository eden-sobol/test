import css from "styled-jsx/css";

const color: string = "green";

export default css`
	main {
		position: absolute;
		top: 0;
		left: 0;
		width: 100vw;
		height: 100vh;
		background: ${color};
	}
`;
